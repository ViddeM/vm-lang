fn main() {
    let a = 1;

    while (a <= 5) {
        print_number(a);
        a = a + 1;

        if (a == 2) {
            print_number(42);
        }

        if (a > 3) {
            print_number(101);
        } else {
            print_number(100);
        }
    }
}
